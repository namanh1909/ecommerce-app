import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

function PublicEvents() {
	return (
		<View>
			<Text>PublicEvents</Text>
		</View>
	);
}

export default PublicEvents;

const styles = StyleSheet.create({});
